# User

* create the file `server/src/models/User.js`
* install `bcryptjs`
  * [`@types/bcryupjs`](https://www.npmjs.com/package/@types/bcryptjs?activeTab=dependencies) is deprecated, `bcryptjs` provide its own type definitions
